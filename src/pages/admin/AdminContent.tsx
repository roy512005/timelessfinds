import React from 'react';

const AdminContent = () => {
    return (
        <div>
            <h2 style={{ fontFamily: 'var(--font-serif)', color: 'var(--accent-gold)', marginBottom: '2rem' }}>Content Manager</h2>
            <p style={{ color: 'var(--text-secondary)' }}>Edit homepage hero text, about page story, and featured items easily.</p>
        </div>
    );
};
export default AdminContent;
