const AdminSettings = () => {
    return (
        <div>
            <h2 style={{ fontFamily: 'var(--font-serif)', color: 'var(--accent-gold)', marginBottom: '2rem' }}>Settings</h2>
            <p style={{ color: 'var(--text-secondary)' }}>Configure global site settings, manage team permissions, and update API endpoints.</p>
        </div>
    );
};
export default AdminSettings;
