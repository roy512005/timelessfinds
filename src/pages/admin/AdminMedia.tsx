const AdminMedia = () => {
    return (
        <div>
            <h2 style={{ fontFamily: 'var(--font-serif)', color: 'var(--accent-gold)', marginBottom: '2rem' }}>Media Library</h2>
            <p style={{ color: 'var(--text-secondary)' }}>Upload and manage product photos, banner images, and gallery content.</p>
        </div>
    );
};
export default AdminMedia;
